/**
 * Offline gas hints (no RPC). Values are intentionally high; replace with `estimateGas`
 * when an RPC URL is wired in later.
 */
export const GAS_APPROVE = 300_000n;
export const GAS_BRIDGE_DEPOSIT = 500_000n;
export const GAS_BRIDGE_WITHDRAW = 400_000n;
