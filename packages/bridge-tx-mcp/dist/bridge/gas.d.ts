/**
 * Offline gas hints (no RPC). Values are intentionally high; replace with `estimateGas`
 * when an RPC URL is wired in later.
 */
export declare const GAS_APPROVE = 300000n;
export declare const GAS_BRIDGE_DEPOSIT = 500000n;
export declare const GAS_BRIDGE_WITHDRAW = 400000n;
